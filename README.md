# Лабораторная работа №1 по C#

Данный проект содержит реализацию 20 заданий (чётные варианты) по четырём темам: методы, условия, циклы и массивы.

## 📋 Содержание

- [Методы](#методы)
- [Условия](#условия)
- [Циклы](#циклы)
- [Массивы](#массивы)
- [Запуск программы](#запуск-программы)

## 🔧 Методы

| № | Метод | Описание |
|---|-------|----------|
| 2 | `sumLastNums(int x)` | Возвращает сумму двух последних цифр числа |
| 4 | `isPositive(int x)` | Проверяет, является ли число положительным |
| 6 | `isUpperCase(char x)` | Проверяет, является ли символ заглавной латинской буквой |
| 8 | `isDivisor(int a, int b)` | Проверяет, делится ли одно число на другое нацело |
| 10 | `lastNumSum(int a, int b)` | Суммирует последние цифры двух чисел |
| 10* | `multiLastNumSum()` | Демонстрирует многократное накопление суммы последних цифр |

## 🔀 Условия

| № | Метод | Описание |
|---|-------|----------|
| 2 | `safeDiv(int x, int y)` | Безопасное деление (возвращает 0 при делении на 0) |
| 4 | `makeDecision(int x, int y)` | Возвращает строку сравнения двух чисел |
| 6 | `sum3(int x, int y, int z)` | Проверяет, можно ли сложить два числа и получить третье |
| 8 | `age(int x)` | Возвращает возраст с правильным склонением |
| 10 | `printDays(string x)` | Выводит все дни недели, начиная с указанного |

## 🔁 Циклы

| № | Метод | Описание |
|---|-------|----------|
| 2 | `reverseListNums(int x)` | Возвращает строку чисел от x до 0 |
| 4 | `pow(int x, int y)` | Возведение x в степень y |
| 6 | `equalNum(int x)` | Проверяет, все ли цифры числа одинаковы |
| 8 | `leftTriangle(int x)` | Выводит левый треугольник из звёздочек |
| 10 | `guessGame()` | Игра "Угадай число" (0-9) |

## 📊 Массивы

| № | Метод | Описание |
|---|-------|----------|
| 2 | `findLast(int[] arr, int x)` | Находит последнее вхождение числа в массиве |
| 4 | `add(int[] arr, int x, int pos)` | Вставляет элемент в массив по индексу |
| 6 | `reverse(int[] arr)` | Разворачивает массив на месте |
| 8 | `concat(int[] arr1, int[] arr2)` | Объединяет два массива |
| 10 | `deleteNegative(int[] arr)` | Удаляет все отрицательные числа из массива |

========================================
    ЛАБОРАТОРНАЯ РАБОТА №1 (чётные)
========================================

Выберите задание:
1 - Задание 1 (Методы)
2 - Задание 2 (Условия)
3 - Задание 3 (Циклы)
4 - Задание 4 (Массивы)
0 - Выход
Ваш выбор: 1

--- ЗАДАНИЕ 1: МЕТОДЫ ---
1) Сумма двух последних цифр числа 4568: 14
2) Проверка положительности:
   isPositive(3) → True
   isPositive(-5) → False
...
